<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="agata91bcomgithub.sdacourseapplication">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".drawing.DrawingMainActivity"
            android:label="DrawingActivity" />
        <!--
     ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <activity
            android:name=".gallery.GalleryActivity"
            android:label="Gallery Activity" />
        <activity
            android:name=".todolist.ToDoListActivity"
            android:label="To do list" />
        <activity
            android:name=".quiz.QuizActivity"
            android:label="Quiz" />
        <activity
            android:name=".book.BooksActivity"
            android:label="Books" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".fortunetale.FortuneActivity"
            android:label="Omen">
            </activity>
    </application>

</manifest>